<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/screen_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    android:keepScreenOn="true"
    android:focusable="true"
    android:focusableInTouchMode="true">

    <!-- ══ TEXTE PRINCIPAL (verset / lyric / annonce) ══ -->
    <!-- padding réduit à 32dp pour maximiser la zone d'affichage -->
    <!-- autoSizeText : le texte se réduit si trop long, monte jusqu'à 120sp -->
    <TextView
        android:id="@+id/tv_verse"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:padding="32dp"
        android:text=""
        android:textColor="#FFFFFF"
        android:textSize="72sp"
        android:lineSpacingMultiplier="1.2"
        android:visibility="invisible"
        android:elevation="4dp"
        app:autoSizeTextType="uniform"
        app:autoSizeMinTextSize="24sp"
        app:autoSizeMaxTextSize="120sp"
        app:autoSizeStepGranularity="2sp" />

    <!-- ══ RÉFÉRENCE / SOUS-TITRE ══ -->
    <TextView
        android:id="@+id/tv_ref"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="16dp"
        android:text=""
        android:textColor="#D97706"
        android:textSize="24sp"
        android:letterSpacing="0.06"
        android:visibility="gone"
        android:elevation="4dp" />

    <!-- ══ LOWER THIRD ══ -->
    <LinearLayout
        android:id="@+id/lt_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:visibility="gone"
        android:alpha="0"
        android:elevation="6dp">

        <View
            android:id="@+id/lt_bar"
            android:layout_width="match_parent"
            android:layout_height="3dp"
            android:background="#D97706" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#CC000000"
            android:orientation="vertical"
            android:paddingStart="48dp"
            android:paddingEnd="48dp"
            android:paddingTop="14dp"
            android:paddingBottom="18dp">

            <TextView
                android:id="@+id/lt_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text=""
                android:textColor="#FFFFFF"
                android:textSize="28sp"
                android:textStyle="bold"
                android:letterSpacing="0.04" />

            <TextView
                android:id="@+id/lt_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text=""
                android:textColor="#D97706"
                android:textSize="18sp"
                android:letterSpacing="0.08"
                android:layout_marginTop="2dp" />
        </LinearLayout>
    </LinearLayout>

    <!-- ══ INDICATEUR IP / SCREEN ID ══ -->
    <TextView
        android:id="@+id/tv_screen_info"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_margin="16dp"
        android:text=""
        android:textColor="#33FFFFFF"
        android:textSize="11sp"
        android:letterSpacing="0.1"
        android:visibility="gone"
        android:elevation="8dp" />

    <!-- ══ STATUT CONNEXION ══ -->
    <TextView
        android:id="@+id/tv_conn_status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_margin="16dp"
        android:text=""
        android:textColor="#EF4444"
        android:textSize="12sp"
        android:letterSpacing="0.06"
        android:visibility="gone"
        android:elevation="8dp" />

</FrameLayout>
